spring:
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    url: jdbc:mysql://127.0.0.1:3306/coke?useUnicode=true&characterEncoding=utf8
    username: root
    password: eunhyuk0404
    driver-class-name: com.mysql.jdbc.Driver
    initialSize: 5
    minIdle: 5
    maxActive: 20
    maxWait: 60000
    timeBetweenEvictionRunsMillis: 60000
    minEvictableIdleTimeMillis: 300000
    validationQuery: SELECT 1 FROM DUAL
    testWhileIdle: true
    testOnBorrow: false
    testOnReturn: false
    poolPreparedStatements: true
    #   配置监控统计拦截的filters，去掉后监控界面sql无法统计，'wall'用于防火墙
    filters: stat,wall,log4j
    maxPoolPreparedStatementPerConnectionSize: 20
    useGlobalDataSourceStat: true
    connectionProperties: druid.stat.mergeSql=true;druid.stat.slowSqlMillis=500

  jpa:
    database: MYSQL
    show-sql: true
    open-in-view: false
    hibernate:
      ddl-auto: update

  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8


    http:
      encoding:
        force: true
        charset: UTF-8
        enabled: true

server:
#  port: 8443
#  ssl:
#    key-store: D:/https/mykeys.jks
#    key-store-password: eunhyuk0404
#    key-password: eunhyuk0404

  tomcat:
    uri-encoding: UTF-8
